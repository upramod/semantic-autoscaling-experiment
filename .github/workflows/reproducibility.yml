name: Reproducibility checks

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  python-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Compile Python sources
        run: python -m compileall -q scaler worker loadgen analysis
      - name: Validate experiment entry points
        run: |
          test -f scripts/run_experiment.sh
          test -f scripts/run_matrix.sh
          bash -n scripts/run_experiment.sh
          bash -n scripts/run_matrix.sh
